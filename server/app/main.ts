import AppModule from "@/app/module"

// async function bootstrap() {
//   const app = await AppModule();
//   await app.listen();
// }

// bootstrap();

const app = await AppModule()

export default app
